@model IEnumerable<Category>

<li class="dropdown">
    <a asp-controller="HairPieces"
       asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Hair pieces<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="HairPieces" asp-action="HairPiecesList"
                   asp-route-category="@category.CategoryName">@category.CategoryName</a>
            </li>
        }
        <li class="divider"></li>
        <li>
            <a asp-controller="HairPieces" asp-action="HairPiecesList" asp-route-Category="">View all hair pieces</a>
        </li>
    </ul>
</li>